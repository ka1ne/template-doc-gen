# Template Processing Configuration
# Directory containing template YAML files
SOURCE_DIR=templates

# Directory where HTML documentation will be generated
OUTPUT_DIR=docs/output

# Output format (currently only HTML is fully supported)
FORMAT=html

# Enable detailed logging
VERBOSE=true

# Validate templates without generating documentation
VALIDATE_ONLY=false

# Integration Note:
# The generator creates static HTML files that can be integrated with any documentation
# system by:
# 1. Uploading to a static web server
# 2. Including in documentation systems like Confluence
# 3. Converting to other formats with tools like Pandoc
# 4. Integrating with CI/CD to auto-generate on commits 